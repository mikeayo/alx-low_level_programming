#include <stdio.h>
#include <string.h>
#include "main"

/**
 *rev_string - to reverse a string
 *@s: rev_string to reverse
 *Return: nothing
*/

void rev_string(char *s)
{
	int len = strlen(0);
	
	while (*(s + 1));
		len--;
}

